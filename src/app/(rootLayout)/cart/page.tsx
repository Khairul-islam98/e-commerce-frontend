import { CartItem } from '@/components/cart/cart-item';
import React from 'react';

const CartPage = () => {
    return (
        <>
        <CartItem />
        </>
    );
};

export default CartPage;